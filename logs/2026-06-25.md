# Log — 2026-06-25

## Objetivo da sessão
Três objetivos:
1. Corrigir página em branco no GitHub Pages.
2. Aplicar os bugs FIX-001 (PDF) e FIX-002 (CSV) que foram gerados mas não aplicados na sessão anterior.
3. Gerar sistema de documentação de contexto completo em `meta/` com referência ao CEREBRO.md (renomeado de CLAUDE.md).

## Feito
- **vite.config.js** — adicionado `base: './'`; corrige URLs absolutas de assets (DEC-007).
- **.github/workflows/deploy.yml** — GitHub Actions: build automático + deploy de `dist/` a cada push na main. Resolve o "ainda em branco" no Pages (o problema real era código-fonte sendo servido sem build).
- **AppContext.jsx (FIX-001)** — PDF armazenado como Blob URL (`URL.createObjectURL`), não ArrayBuffer. `closeFile` revoga a URL. Elimina o erro `"ArrayBuffer at index 0 is already detached"` do PDF.js v4.
- **PdfViewer.jsx (FIX-001)** — passa Blob URL string diretamente para `lib.getDocument(url)`.
- **CsvViewer.jsx (FIX-002)** — injeção de `__dataIdx` em cada linha do `filtered`; `commitEdit` opera sobre `data` original. Editar com filtro ativo não descarta mais linhas.
- **CsvViewer.jsx** — botão ✕ cancelar edição inline (`onMouseDown + preventDefault`). Botão "✕ limpar" no campo de filtro.
- **meta/ consolidado** — duplicatas eliminadas (CHANGELOG (2)/(3), IDEAS (2)/(3), ROADMAP (2)/(3), STATUS (2)). Um único arquivo canônico de cada.
- 9 documentos de contexto gerados/atualizados em `meta/`.
- `logs/2026-06-25.md` — este arquivo.

## Decisões
- **DEC-007** — `base: './'` no vite.config.js + GitHub Actions para deploy correto no Pages. Ver DECISIONS.md.
- CEREBRO.md confirmado como nome do arquivo de instrução (renomeado de CLAUDE.md); referência atualizada em todos os docs.

## Bugs
- **FIX-001** — PDF ArrayBuffer detachado pelo worker do PDF.js v4. Solução: Blob URL. Arquivos: AppContext.jsx + PdfViewer.jsx.
- **FIX-002** — CSV edição com filtro ativo descartava linhas fora do filtro. Solução: `__dataIdx` + operar sobre `data` original. Arquivo: CsvViewer.jsx.

## Aprendizados / armadilhas
- GitHub Pages em branco sem erro óbvio → dois motivos: `base` ausente no Vite + código-fonte servido sem build. Solução completa: `base: './'` + GitHub Actions. → Armadilha #6 CONTEXT.md + HISTORICO.md seção 3.
- PDF.js v4 transfere o ArrayBuffer para o worker — nunca armazenar ArrayBuffer de PDF em estado React. → Armadilha #1 CONTEXT.md.
- `onBlur` sempre dispara antes de `onClick`; para cancelar via botão, usar `onMouseDown + preventDefault`. → Armadilha #5 CONTEXT.md.

## Onde parei
Todos os 4 arquivos de código corrigidos, deploy.yml criado, meta/ limpo (9 arquivos canônicos), log criado. ZIP entregue. Próximo passo: atacar **JSON modo formulário/cards** (maior impacto visual na F2 — maior pedido pendente do usuário).

## Próximos passos
- [ ] Aplicar os arquivos do ZIP no projeto.
- [ ] **Configurar GitHub Pages:** Settings → Pages → Source → GitHub Actions.
- [ ] Fazer `git push` e aguardar o Actions rodar (~2 min). URL aparece em Settings → Pages.
- [ ] Confirmar que o app abre corretamente no URL do Pages.
- [ ] Atacar JSON modo formulário/cards: cada chave de 1º nível vira seção; arrays de objetos viram tabela.
